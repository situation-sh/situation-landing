# Situation landing page

## Development

Run `npm install` and `npm run dev`, then simply open `./src/index.html`. Refresh whenever changes are made.

## Publich

Simply host the `src` directory to a static web server.
